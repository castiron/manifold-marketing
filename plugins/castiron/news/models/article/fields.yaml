# ===================================
#  Form Field Definitions
# ===================================

tabs:
  fields:
    id:
      label: ID
      disabled: true
      tab: Content
    # title:
    #   label: Title
    #   required: true
    #   span: auto
    #   tab: Content
    # author:
    #   label: Author
    #   span: auto
    #   tab: Content
    date:
      label: Date
      required: true
      type: datepicker
      span: auto
      tab: Content
    teaser:
      label: Announcement
      span: auto
      type: textarea
      size: small
      tab: Content
    # body:
    #   label: Bodytext
    #   type: richeditor
    #   size: huge
    #   tab: Content
    slug:
        label: 'URL slug'
        span: auto
        required: 1
        type: text
        tab: Content

    # thumbnail:
    #   label: Thumbnail
    #   commentAbove: Appears in the list view.
    #   type: mediafinder
    #   mode: image
    #   span: right
    #   tab: Media
    # interior_image:
    #   label: Primary Image
    #   commentAbove: Appears in the article detail view.
    #   type: mediafinder
    #   mode: image
    #   span: left
    #   tab: Media

    visibility_section:
        tab: Visibility
        type: section
        comment: When a given user is logged into the back end, this detail view will remain visible to them for editing.
    is_hidden:
        tab: Visibility
        label: Article is Hidden
        type: checkbox
        comment: If checked, article will not be visible to users. Overrides all other visibility settings.
    startdate:
        tab: Visibility
        label: Visibility Start
        type: datepicker
        comment: If set, article will not be visible to users before the specified date/time.
    enddate:
        tab: Visibility
        label: Visibility End
        type: datepicker
        comment: If set, article will not be visible to users after the specified date/time.
