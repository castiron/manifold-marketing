# ===================================
#  Form Field Definitions
# ===================================

fields:
  type:
    label: Type
    type: dropdown
    options:
      summarized: Summarized
      tabbed: Tabbed
  title:
    label: Title
    type: text
    trigger:
      action: hide
      field: type
      condition: value[tabbed]
  subtitle:
    label: Subtitle
    type: text
    trigger:
      action: hide
      field: type
      condition: value[tabbed]
